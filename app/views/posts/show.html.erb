  
<!-- Breadcrumb nav -->
  <nav>
    <div class="nav-wrapper sub-nav">
      <div class="col s12">
        <span href="#!" class="breadcrumb"> <%= link_to 'All Cities', cities_path %> </span>
        <span class="breadcrumb"><%= link_to @city_object.name, city_path(@city) %> </span>
        <a  class="breadcrumb"> <%= @post.title %> </a>        
      </div>
    </div>
  </nav>



<div class="container">
  <div class="card form_card">
    <h1>Posts#show</h1>

    <h5>title:</h5>
    <%= @post.title %>
    <br>
    <h5>content:</h5>
    <%= @post.content %>
    <br>
    <%= link_to 'Add Comment', new_city_post_comment_path(@city, @post), class: "btn-mini btn btn-primary btn-small green lighten-3"%>
    <%= link_to "Edit",  edit_city_post_path(@post), class: "btn btn-primary project-btn right" %>
    <br>
    <br>
    <div class="card form_card">
    <p><small>Comments:</small></p>
    <br>
    <% @comments.each do |comment| %>
    <div class="card form_card">
    <p><small> <%= comment.user.email %> : </small></p>
     <p> "<%= comment.content %>" </p>
     </div>
    <% end %>
    
    </div>
    

 </div>
</div>